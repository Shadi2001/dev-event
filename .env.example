# Local SQLite database (no external DB required; path relative to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# App URL for server-side fetch (e.g. homepage, event details)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Optional: Cloudinary (for POST /api/events image uploads)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
